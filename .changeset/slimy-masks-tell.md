---
"@openid4vc/openid4vci": minor
---

refactor: To support credential response encryption, the `createCredentialResponse` now returns an object containing the `credentialResponse` parameter and optionally a `credentialResponseJwt`.

